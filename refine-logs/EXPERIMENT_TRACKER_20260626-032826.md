# Experiment Tracker

Run config: `configs/tccig/01.yaml` (stabilized) · Launch: `scripts/tccig.sh` · Benchmark: PRING Human/BFS

| Run ID | Milestone | Purpose | System / Variant | Split | Metrics | Priority | Status | Notes |
|--------|-----------|---------|------------------|-------|---------|----------|--------|-------|
| R000 | — | Attempt 1: full run (pre-fix). FAILED — refiner residual exploded (train_residual_anchor ~7e14, grad_norm ~4.7e14); refined output collapsed (85% exactly 0.0, AUPRC 0.49 vs raw 0.79). `val_topology_loss` selected epoch 13 (val_auprc 0.12). | `01.yaml` pre-fix, slurm 928779, 40 epochs, 4× A40 | train/val → test | — | — | DONE (FAILED) | Root cause: unnormalized GraphConv (`aggr=add`) + product decoder squared magnitudes + residual_weight 1e-3 too weak. Artifacts: `artifacts/tccig_01_pairwise_debug_20260626/`. Fix landed in `tccig/s2gae.py`. |
| R001 | M0 | Sanity: stabilized pipeline runs end-to-end, IO/metric contract correct, scorer cache populates, self-pairs dropped, raw+refined artifacts written | `01.yaml` fork, `epochs: 2`, 1 GPU | train/val (short) | edge counts, NaN check, artifact presence (`raw_metrics.json`, `refined_metrics.json`) | MUST | TODO | Verify `pairwise_test` + `topology_test` artifacts write; τ_pair reaches precision ≥ 0.8 on val |
| R002 | G0 | Stability gate: confirm fix holds on real dense `G_pairwise` before the 4-day M2 | `01.yaml`, `epochs: 5–8`, 1–2 GPU | train/val → (cheap test) | per-epoch `train_residual_anchor_loss` (must stay O(1–100)), grad_norm finite, train BCE + val_topology_loss trend down, refined AUPRC ≥ raw AUPRC floor | MUST | TODO | **New gate.** Hard stop if anchor explodes or refined collapses. Cheap insurance vs repeating R000. |
| R003 | M1 | Reproduce frozen pairwise baseline + its topology metrics | v3.1 scorer @ `logs/tccig/pairwise_baseline` | test | precision/recall/F1/AUPRC; graph_sim, rel_density, deg/cc/spectral MMD | MUST | TODO | Artifact not present locally — confirm on HPC; regenerate from frozen scorer if absent |
| R004 | M2 | Main method: full stabilized refiner training to convergence | `01.yaml`, 40 epochs, 4× A40 DDP | train/val → test | monitor `val_topology_loss` + AUPRC-floor guard; test pairwise (raw+refined) + topology | MUST | TODO | Primary run (Block 1 / C1). Only launch after G0 passes. Plot monitor vs test topology; check selected checkpoint passes refined-AUPRC floor |
| R005 | M3 | Anti-threshold isolation: no scorer threshold matches refiner topology | Frozen scorer threshold sweep (post-hoc on cached scores) | test | topology summary at density/precision-matched points | MUST | TODO | Block 2 / C2. Fix matching variable (density vs precision) up front; CPU recompute |
| R006 | M4 | Per-node-size tables, topology curves, refined-output-threshold sensitivity | Re-use R004 outputs | test | per-node-size topology details | NICE | TODO | Appendix evidence; no new training |
| R007 | M4 | Deletion study (a): − bounded residual / − residual anchor | `02.yaml` fork, `residual_scale: 0` (unbounded) and/or `residual_weight: 0` | train/val → test | topology + pairwise deltas vs R004; stability | NICE | TODO | Doubles as stabilization ablation — shows the fix is load-bearing. Defer to config `02` |
| R008 | M4 | Deletion study (b): − scorer-error sampling (exhaustive train-pair BCE) | `02.yaml` fork, pre-ADR-002 contract | train/val → test | topology + pairwise deltas vs R004 | NICE | TODO | Defer to config `02` |
| R009 | M4 | Deletion study (c): − cross-layer decoder (inner-product decode) | `02.yaml` fork, simple decoder | train/val → test | topology + pairwise deltas vs R004 | NICE | TODO | Defer to config `02` |

Status legend: TODO · RUNNING · DONE · DONE (FAILED) · BLOCKED
